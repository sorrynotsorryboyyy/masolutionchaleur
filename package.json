{
  "name": "masolutionchaleur",
  "version": "2.0.0",
  "private": true,
  "description": "Landing d'acquisition de leads chauffage — masolutionchaleur.fr",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o css/style.css --minify",
    "watch": "tailwindcss -i src/input.css -o css/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
